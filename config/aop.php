<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | AOP 启用状态
    |--------------------------------------------------------------------------
    |
    | 设置是否启用 AOP 功能
    |
    */
    'enable' => true,

    /*
    |--------------------------------------------------------------------------
    | 运行模式
    |--------------------------------------------------------------------------
    |
    | 支持两种模式：
    | - proxy: 运行时动态代理（默认）
    | - compile: 编译时代码织入
    |
    */
    'mode' => 'proxy',

    /*
    |--------------------------------------------------------------------------
    | 缓存配置
    |--------------------------------------------------------------------------
    |
    | 配置代理类的缓存方式
    |
    */
    'cache' => [
        'driver' => 'file', // 支持 file, apcu, redis
        'path' => storage_path('aop/cache') ?? sys_get_temp_dir() . '/aop_cache',
    ],

    /*
    |--------------------------------------------------------------------------
    | 切面类列表
    |--------------------------------------------------------------------------
    |
    | 注册需要使用的切面类
    |
    */
    'aspects' => [
        // App\Aspect\LoggingAspect::class,
        // App\Aspect\TransactionAspect::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | 切入点表达式
    |--------------------------------------------------------------------------
    |
    | 定义可复用的切入点表达式
    |
    */
    'pointcuts' => [
        // 'service_methods' => 'execution(* App\Service\*->*())',
    ],
];